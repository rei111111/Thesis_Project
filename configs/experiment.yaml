seed: 42

data:
  dataset_path: data/dataset.csv
  train_indices_path: data/train_indices.csv
  test_indices_path: data/test_indices.csv
  excluded_indices_path: data/excluded_indices.csv
  split_manifest_path: data/split_manifest.json
  validation_report_path: data/validation_report.json
  target_column: label
  expected_rows: 248

split:
  # The authors are responsible for resolving repeated source rows before the
  # definitive thesis run. The code performs ordinary label stratification.
  seed: 42
  test_size: 0.20

features:
  linguistic_lexicon_path: configs/linguistic_lexicons.yaml
  tfidf:
    max_features: 300
    ngram_range: [1, 2]
    stop_words: english
  raw_engagement_columns:
    - likes
    - comments
    - views
    - duration_sec

ridge:
  alphas: [0.1, 1.0, 10.0]

minilm:
  model_name: sentence-transformers/all-MiniLM-L6-v2
  batch_size: 32
  c_values: [0.1, 1.0, 10.0]

evaluation:
  outer_folds: 5
  inner_folds: 3
  bootstrap_resamples: 10000
  primary_metric: quadratic_weighted_kappa
  n_jobs: 1
